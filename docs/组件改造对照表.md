# 🔄 组件改造对照表

快速查找旧代码对应的 Tailwind + Framer Motion 新写法。

---

## 1️⃣ Button（按钮）

### 改造前
```tsx
<button className="button primary">点击我</button>
<button className="button secondary">取消</button>
<button className="button accent">重要</button>
```

### 改造后
```tsx
import { Button } from '@/components/ui/button'

<Button variant="primary">点击我</Button>
<Button variant="secondary">取消</Button>
<Button variant="default">重要</Button>
```

### 对应类名
```css
旧类名              → 新类名
.button             → inline-flex items-center justify-center rounded-xl font-medium
.button.primary     → bg-primary-500 text-white hover:bg-primary-600
.button.secondary   → bg-neutral-100 text-neutral-900 hover:bg-neutral-200
.button.ghost       → bg-transparent hover:bg-neutral-100
.button.accent      → bg-primary-500
```

---

## 2️⃣ Card（卡片）

### 改造前
```tsx
<div className="card">
  <h2>标题</h2>
  <p>内容</p>
</div>
```

### 改造后
```tsx
import { Card } from '@/components/ui/card'

<Card>
  <h2>标题</h2>
  <p>内容</p>
</Card>
```

### 对应类名
```css
旧类名    → 新类名
.card    → bg-white rounded-2xl p-6 shadow-soft border border-neutral-200
```

---

## 3️⃣ Input（输入框）

### 改造前
```tsx
<input className="input" placeholder="请输入..." />
```

### 改造后
```tsx
import { Input } from '@/components/ui/input'

<Input placeholder="请输入..." />
```

### 对应类名
```css
旧类名    → 新类名
.input   → h-11 w-full rounded-xl border-2 border-neutral-200 bg-white px-4
          → focus:ring-2 focus:ring-primary-500 focus:border-primary-500
```

---

## 4️⃣ Badge（徽章）

### 改造前
```tsx
<span className="badge">标签</span>
```

### 改造后
```tsx
import { Badge } from '@/components/ui/badge'

<Badge>标签</Badge>
<Badge variant="success">成功</Badge>
<Badge variant="warning">警告</Badge>
```

### 对应类名
```css
旧类名    → 新类名
.badge   → inline-flex items-center rounded-full px-3 py-1 text-xs font-semibold
         → bg-neutral-100 text-neutral-900
```

---

## 5️⃣ Grid（网格布局）

### 改造前
```tsx
<div className="grid two">
  <div>项目1</div>
  <div>项目2</div>
</div>

<div className="grid three">
  <div>项目1</div>
  <div>项目2</div>
  <div>项目3</div>
</div>
```

### 改造后
```tsx
<div className="grid grid-cols-1 md:grid-cols-2 gap-4">
  <div>项目1</div>
  <div>项目2</div>
</div>

<div className="grid grid-cols-1 md:grid-cols-3 gap-4">
  <div>项目1</div>
  <div>项目2</div>
  <div>项目3</div>
</div>
```

### 对应类名
```css
旧类名              → 新类名
.grid              → grid gap-4
.grid.two          → grid-cols-1 md:grid-cols-2
.grid.three        → grid-cols-1 md:grid-cols-3
```

---

## 6️⃣ Flex（弹性布局）

### 改造前
```tsx
<div className="flex">
  <span>左侧</span>
  <span>右侧</span>
</div>

<div className="flex space">
  <span>左侧</span>
  <span>右侧</span>
</div>
```

### 改造后
```tsx
<div className="flex items-center gap-3">
  <span>左侧</span>
  <span>右侧</span>
</div>

<div className="flex items-center justify-between">
  <span>左侧</span>
  <span>右侧</span>
</div>
```

### 对应类名
```css
旧类名        → 新类名
.flex        → flex items-center gap-3
.flex.space  → flex items-center justify-between
```

---

## 7️⃣ Text（文本样式）

### 改造前
```tsx
<h2 className="section-title">标题</h2>
<p className="muted">次要文本</p>
<p className="small">小字</p>
```

### 改造后
```tsx
<h2 className="text-xl font-semibold mb-3">标题</h2>
<p className="text-sm text-neutral-600">次要文本</p>
<p className="text-xs">小字</p>
```

### 对应类名
```css
旧类名          → 新类名
.section-title  → text-xl font-semibold mb-3
.muted         → text-sm text-neutral-600
.small         → text-xs
```

---

## 8️⃣ Modal（模态框）

### 改造前
```tsx
<div className="modal-overlay">
  <div className="modal">
    <h2>标题</h2>
    <p>内容</p>
  </div>
</div>
```

### 改造后
```tsx
import { Modal } from '@/components/ui/modal'

<Modal isOpen={show} onClose={() => setShow(false)}>
  <h2>标题</h2>
  <p>内容</p>
</Modal>
```

### 对应类名
```css
旧类名          → 新类名
.modal-overlay  → fixed inset-0 bg-black/50 backdrop-blur-sm z-50
.modal         → relative w-full max-w-lg rounded-2xl bg-white p-6 shadow-hard
```

---

## 9️⃣ ProgressBar（进度条）

### 改造前
```tsx
<div className="progress-bar">
  <div style={{ width: '50%' }}></div>
</div>
```

### 改造后
```tsx
import { ProgressBar } from '@/components/ui/progress-bar'

<ProgressBar value={50} max={100} />
```

### 对应类名
```css
旧类名          → 新类名
.progress-bar  → w-full h-2 rounded-full bg-neutral-100 overflow-hidden
> div          → h-full bg-primary-500 transition-all duration-500
```

---

## 🔟 SplitLayout（分栏布局）

### 改造前
```tsx
<div className="split-layout">
  <div>左侧</div>
  <div>右侧</div>
</div>
```

### 改造后
```tsx
<div className="grid grid-cols-1 lg:grid-cols-5 gap-6">
  <div className="lg:col-span-2">左侧</div>
  <div className="lg:col-span-3">右侧</div>
</div>
```

### 对应类名
```css
旧类名          → 新类名
.split-layout  → grid grid-cols-1 lg:grid-cols-5 gap-6
                → lg:grid-cols-[40%_60%]
```

---

## 1️⃣1️⃣ Form Elements（表单元素）

### 改造前
```tsx
<div className="form-grid">
  <div>
    <label className="label">邮箱</label>
    <input className="input" />
  </div>
</div>
```

### 改造后
```tsx
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'

<div className="space-y-2">
  <Label>邮箱</Label>
  <Input />
</div>
```

---

## 1️⃣2️⃣ Animations（动画）

### 页面入场动画
```tsx
// 改造前：无动画

// 改造后：
import { motion } from 'framer-motion'

<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.5 }}
>
  {children}
</motion.div>
```

### 悬停效果
```tsx
// 改造前：无悬停效果

// 改造后：
<motion.div
  whileHover={{ y: -4 }}
  whileTap={{ scale: 0.98 }}
>
  {children}
</motion.div>
```

### 列表交错动画
```tsx
// 改造前：无动画

// 改造后：
{items.map((item, index) => (
  <motion.div
    key={item.id}
    initial={{ opacity: 0, y: 20 }}
    animate={{ opacity: 1, y: 0 }}
    transition={{ delay: index * 0.1 }}
  >
    {item.content}
  </motion.div>
))}
```

---

## 🎨 颜色对照表

### 品牌色
```css
旧变量              → 新类名
--accent            → primary-500
--accent-dark       → primary-600
```

### 中性色
```css
旧变量              → 新类名
--text             → neutral-900
--muted            → neutral-600
--bg               → neutral-50
--card             → white
--border           → neutral-200
```

---

## 📐 尺寸对照表

### 间距
```css
旧代码      → Tailwind
gap: 16px  → gap-4
gap: 24px  → gap-6
padding: 20px → p-5
margin: 32px 0 → my-8
```

### 圆角
```css
旧代码              → Tailwind
border-radius: 18px → rounded-2xl
border-radius: 999px → rounded-full
```

---

## 🚀 快速迁移命令

### 1. 运行自动化脚本
```bash
chmod +x setup-frontend.sh
./setup-frontend.sh
```

### 2. 手动迁移（推荐逐个组件）
```bash
# 1. 创建新组件
touch components/ui/button.tsx

# 2. 从对照表复制新代码
# 3. 更新引用
# 4. 测试功能
# 5. 删除旧代码
```

---

## 📝 迁移检查清单

### 阶段 1：基础组件（Week 1）
- [ ] Button 组件
- [ ] Card 组件
- [ ] Input 组件
- [ ] Badge 组件
- [ ] Label 组件
- [ ] Loading 组件

### 阶段 2：布局组件（Week 2）
- [ ] Header 组件
- [ ] Footer 组件
- [ ] Modal 组件
- [ ] ProgressBar 组件
- [ ] Grid 系统

### 阶段 3：页面改造（Week 3）
- [ ] 首页（题库广场）
- [ ] 刷题页面
- [ ] 错题本页面
- [ ] 管理面板
- [ ] 登录/注册页面

### 阶段 4：动画与细节（Week 4）
- [ ] 页面过渡动画
- [ ] 悬停效果
- [ ] 列表交错动画
- [ ] 加载骨架屏
- [ ] Toast 通知

---

## 💡 最佳实践

1. **渐进式迁移**：一次迁移一个组件，测试通过后再继续
2. **保留旧代码**：在确认新代码稳定前，不要删除旧代码
3. **测试覆盖**：每个组件迁移后都要测试
4. **文档同步**：及时更新组件使用文档

---

**开始迁移，让你的应用焕然一新！** 🎉
